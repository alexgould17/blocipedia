<% if policy(@wiki).show? %>
  <%= render @wiki %>
  <br>
  <%= link_to "Edit This Wiki", edit_wiki_path if policy(@wiki).edit? %><br>
  <%= link_to "Delete This Wiki", @wiki, method: :delete if policy(@wiki).destroy? %>
<% else %>
  <em>You are not authorized to view this wiki!</em>
<% end %>
